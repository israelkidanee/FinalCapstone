const express = require("express");
const app = express();

import "./app.css";

function app() {
  return (
    <div className="App">
      <header className="App-header">
        <p>Learn React</p>
      </header>
    </div>
  );
}

export default app;
module.exports = app;
